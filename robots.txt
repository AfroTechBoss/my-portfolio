User-agent: *
Allow: /

# /src and /styles stay crawlable on purpose: every page is rendered in the
# browser from those JSX files, so a crawler blocked from them sees an empty
# <div id="root"> and nothing else.
#
# The .html twins of the clean URLs are deliberately NOT disallowed either.
# Each one carries a rel="canonical" pointing at the clean URL, and a crawler
# that is blocked from fetching a page can never read the canonical inside it.

Sitemap: https://www.chidile.xyz/sitemap.xml
